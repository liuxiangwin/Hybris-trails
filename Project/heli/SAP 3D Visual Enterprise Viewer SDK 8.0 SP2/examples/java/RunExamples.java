package com.sap.visualenterprise.examples;

public class RunExamples {
	public static void main(String[] args)
	{
		// Runs all examples in sequence. Comment out the lines of examples that you do not wish to run:
		new GettingStarted_Empty(); 
		new GettingStarted_LoadFile(); 
		new AssemblyLoading();
		new EventHandling();
		new FileSaving();
		new GetScreenshot();
		new FindMetadata();
		new TranslateRotateScale();
	}
}